# react-timer
 Basic Timer with React

This project was created with **React** and **React-Bootstrap** as my first learning project with React.

Currently still in development build.<br />
Run `npm start` and open [http://localhost:3000](http://localhost:3000) to view it in the browser.
